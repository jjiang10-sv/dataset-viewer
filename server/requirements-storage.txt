# AWS Storage Dependencies for Dataset Viewer
# Install with: pip install -r requirements-storage.txt

# Core AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Additional useful packages for AWS
python-dotenv>=1.0.0  # For environment variable management
tenacity>=8.2.0       # For retry logic with AWS services
aioboto3>=12.3.0      # Async boto3 (optional, for better async performance)

# Compression and serialization
gzip-reader>=0.1.0    # Enhanced gzip handling (optional)
orjson>=3.9.0         # Faster JSON serialization (optional)

# Monitoring and logging (optional but recommended)
structlog>=23.2.0     # Better logging for production
prometheus-client>=0.19.0  # Metrics collection

# Development and testing dependencies (optional)
moto>=4.2.0           # Mock AWS services for testing
pytest-asyncio>=0.21.0  # Async testing support 